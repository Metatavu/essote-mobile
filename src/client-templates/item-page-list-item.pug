li.content-page.list-group-item.list-link(data-level=item.level, data-id=item.id)
  .row
    if item.icon
      .col-1
        i.link-icon(class=item.icon)
    .col
      !=item.name
    .col-1
      i.fa.fa-angle-right